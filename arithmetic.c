#include<stdio.h>
void main()
{
    int a=90,b=102;
    int result;

    result=a+b;
    printf("the adittion of num is:%d",result);

    result=a-b;
    printf("The subraction of two num is: %d",result);

    result=a*b;
    printf("The multiplication of two num is:%d",result);

    result=a/b;
    printf("The division of two numbers is : %d",result);

    result=a%b;
    printf("The modulus of two number is :%d",result);

}
